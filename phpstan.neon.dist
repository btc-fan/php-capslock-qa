parameters:
    # Level 5 catches undefined methods, unknown classes, and bad calls without
    # the missing-type noise Codeception's generated actor would raise at level 6.
    level: 5
    paths:
        - tests
